# Flask Configuration
# These are typically used for development with the 'flask' command.
FLASK_APP=app.py
FLASK_DEBUG=True

# Celery Configuration (using Redis)
# Make sure your Redis server is running on this address.
CELERY_BROKER_URL='redis://localhost:6379/0'
CELERY_RESULT_BACKEND='redis://localhost:6379/0'

# Database Configuration
# Connection string for your PostgreSQL database.
# Format: postgresql://<user>:<password>@<host>:<port>/<database_name>
DATABASE_URL='postgresql://postgres:your_db_password@localhost:5432/scanner_db'

# --- Optional: Target Credentials for DVWA ---
# These are kept for reference but are not directly used by the new scanner architecture.
# Authentication is now handled by providing a session cookie in the UI.
TARGET_BASE_URL="http://localhost/dvwa/"
TARGET_LOGIN_URL="http://localhost/dvwa/login.php"
TARGET_SECURITY_URL="http://localhost/dvwa/security.php"
TARGET_USERNAME="admin"
TARGET_PASSWORD="password"